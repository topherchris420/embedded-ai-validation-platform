# Issue Tracker Configuration

- **Type**: Local markdown files
- **Location**: `.scratch/<feature>/issues/`
- **Convention**: One markdown file per ticket, specifying blocking edges as dependencies.
